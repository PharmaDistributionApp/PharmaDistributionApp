<Window x:Class="PharmaDistributionApp.Views.ChiTietHoaDonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="CHI TIẾT HÓA ĐƠN" Height="700" Width="900"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="White"
        MouseDown="Window_MouseLeftButtonDown" KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style x:Key="RightAlignCell" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="CenterAlignCell" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#4C70BA" BorderThickness="1" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#4C70BA" MouseDown="Header_MouseLeftButtonDown">
                <TextBlock Text="CHI TIẾT HÓA ĐƠN" Foreground="White" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" Margin="20,0"/>
                <Button x:Name="btnCloseHeader" Click="btnClose_Click" Width="40" Height="40" HorizontalAlignment="Right" Margin="0,0,5,0" Style="{StaticResource MaterialDesignIconForegroundButton}" Foreground="White">
                    <materialDesign:PackIcon Kind="Close" Width="24" Height="24"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="THÔNG TIN ĐỐI TÁC" Foreground="#4C70BA" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <materialDesign:PackIcon Kind="Account" Foreground="#666" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtTenDoiTac" Text="---" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <materialDesign:PackIcon Kind="MapMarker" Foreground="#666" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtDiaChi" Text="---" TextWrapping="Wrap" MaxWidth="400" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <materialDesign:PackIcon Kind="Phone" Foreground="#666" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtSDT" Text="---" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountTie" Foreground="#666" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Người lập: " Foreground="Gray"/>
                        <TextBlock x:Name="txtNhanVien" Text="---" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Background="#F9FAFB" CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Mã hóa đơn:" Foreground="Gray" Margin="0,0,0,8"/>
                        <TextBlock x:Name="txtMaHD" Text="---" FontWeight="Bold" Grid.Column="1" HorizontalAlignment="Right"/>

                        <TextBlock Text="Ngày lập:" Grid.Row="1" Foreground="Gray" Margin="0,0,0,8"/>
                        <TextBlock x:Name="txtNgayLap" Text="---" FontWeight="Bold" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right"/>

                        <TextBlock Text="Trạng thái:" Grid.Row="2" Foreground="Gray" Margin="0,0,0,8" VerticalAlignment="Center"/>
                        <Border x:Name="bdTrangThai" Grid.Row="2" Grid.Column="1" Background="#FEEBC8" CornerRadius="12" Padding="10,4" HorizontalAlignment="Right">
                            <TextBlock x:Name="txtTrangThai" Text="---" Foreground="#C05621" FontWeight="Bold" FontSize="11"/>
                        </Border>

                        <TextBlock Text="Ghi chú:" Grid.Row="3" Foreground="Gray" Margin="0,8,0,0"/>
                        <TextBlock x:Name="txtGhiChu" Grid.Row="3" Grid.Column="1" Text="(Không có)" HorizontalAlignment="Right" FontStyle="Italic" Foreground="Gray" Margin="0,8,0,0" TextWrapping="Wrap" MaxWidth="180" TextAlignment="Right"/>
                    </Grid>
                </Border>
            </Grid>

            <DataGrid x:Name="dgChiTiet" Grid.Row="2" Margin="20,0" 
                      AutoGenerateColumns="False" CanUserAddRows="False" 
                      IsReadOnly="True" HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal" Background="#F5F5F5" 
                      BorderThickness="0" CanUserSortColumns="False">
                <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            Background="{TemplateBinding Background}" 
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="#555"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="50" ElementStyle="{StaticResource CenterAlignCell}"/>
                    <DataGridTextColumn Header="Mã SP" Binding="{Binding MaSP}" Width="80" ElementStyle="{StaticResource CenterAlignCell}"/>
                    <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenSP}" Width="*"/>
                    <DataGridTextColumn Header="ĐVT" Binding="{Binding DonVi}" Width="80" ElementStyle="{StaticResource CenterAlignCell}"/>
                    <DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="60" ElementStyle="{StaticResource CenterAlignCell}"/>
                    <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia, StringFormat={}{0:N0}}" Width="120" ElementStyle="{StaticResource RightAlignCell}"/>
                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0}}" Width="140" FontWeight="Bold" ElementStyle="{StaticResource RightAlignCell}"/>
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="3" Margin="20,15,20,0">
                <StackPanel HorizontalAlignment="Left">

                    <Grid Width="500" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Tiền hàng:" Foreground="Gray" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtTienHang" Text="0 VND" FontWeight="Bold" Grid.Column="1" HorizontalAlignment="Left" FontSize="14"/>
                    </Grid>

                    <Grid Width="500" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="lblVAT" Text="VAT (10%):" Foreground="Gray" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtVAT" Text="0 VND" FontWeight="Bold" Grid.Column="1" HorizontalAlignment="Left" FontSize="14"/>
                    </Grid>

                    <Separator Width="500" HorizontalAlignment="Left" Margin="0,5"/>

                    <Grid Width="500">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="TỔNG CỘNG:" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtTongTien" Text="0 VND" FontWeight="Bold" FontSize="24" Foreground="#E53E3E" Grid.Column="1" HorizontalAlignment="Left"/>
                    </Grid>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="4" Margin="20,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

                    <StackPanel x:Name="pnlApprovalButtons" Orientation="Horizontal" Visibility="Collapsed" Margin="0,0,10,0">
                        <Button x:Name="btnReject" Click="btnReject_Click" Content="TỪ CHỐI" Background="#E57373" BorderBrush="#E57373" Width="100" Margin="0,0,10,0"/>
                        <Button x:Name="btnApprove" Click="btnApprove_Click" Content="DUYỆT ĐƠN" Background="#4CAF50" BorderBrush="#4CAF50" Width="120"/>
                    </StackPanel>

                    <Button x:Name="btnExportPDF" Click="btnExportPDF_Click" Background="#D32F2F" BorderBrush="{x:Null}" Width="130">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilePdfBox" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Xuất PDF" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Click="btnClose_Click" Content="Đóng" Margin="10,0,0,0" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="Gray" Foreground="Gray" Width="100"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>