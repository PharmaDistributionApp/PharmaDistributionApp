<Window x:Class="PharmaDistributionApp.Views.ProductView.ChiTietPhieuNhapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi Tiết Phiếu Nhập" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F3F5F9">
    <Grid Margin="20" Background="Transparent" MouseDown="Grid_MouseDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="txtTieuDe" Text="PHIẾU NHẬP KHO: ---" FontSize="24" FontWeight="Bold" Foreground="#1565C0"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Ngày lập: " FontWeight="Bold" Foreground="Gray"/>
                <TextBlock x:Name="txtNgayLap" Text="---" FontWeight="SemiBold" Margin="0,0,20,0"/>
                <TextBlock Text="Trạng thái: " FontWeight="Bold" Foreground="Gray"/>
                <TextBlock x:Name="txtTrangThai" Text="---" FontWeight="Bold" Foreground="#2E7D32"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Số Hóa Đơn Nhập:" FontWeight="Bold" Foreground="Gray"/>
                    <TextBlock x:Name="txtSoHD" Text="---" FontSize="16" Margin="0,5,0,15"/>
                    <TextBlock Text="Nhà Cung Cấp:" FontWeight="Bold" Foreground="Gray"/>
                    <TextBlock x:Name="txtNCC" Text="---" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Kho Nhập:" FontWeight="Bold" Foreground="Gray"/>
                    <TextBlock x:Name="txtKho" Text="---" FontSize="16" Margin="0,5,0,15"/>
                    <TextBlock Text="Nhân viên lập:" FontWeight="Bold" Foreground="Gray"/>
                    <TextBlock x:Name="txtNhanVien" Text="---" FontSize="16"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="dgvChiTiet" 
                  Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False" 
                  CanUserSortColumns="False" 
                  Background="White" 
                  RowHeight="45"
                  GridLinesVisibility="None" 
                  BorderThickness="0" 
                  SelectionMode="Single" 
                  SelectionUnit="FullRow">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#4C70BA"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="BorderBrush" Value="#6B92D8"/>
                </Style>

                <Style TargetType="DataGridCell">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="Transparent" BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F5F5F5"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#EEEEEE"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SP" Binding="{Binding Masp}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#4C70BA"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Tên Sản Phẩm" Binding="{Binding Tensp}" Width="*">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="15,0,0,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="ĐVT" Binding="{Binding Dvt}" Width="80"/>
                <DataGridTextColumn Header="Số Lô" Binding="{Binding Sohieu}" Width="100"/>
                <DataGridTextColumn Header="Hạn Dùng" Binding="{Binding Hsd}" Width="110"/>
                <DataGridTextColumn Header="SL" Binding="{Binding Soluong}" Width="70" FontWeight="Bold"/>
                <DataGridTextColumn Header="Đơn Giá" Binding="{Binding Dongianhap, StringFormat={}{0:N0}}" Width="110"/>
                <DataGridTextColumn Header="Thành Tiền" Binding="{Binding Thanhtien, StringFormat={}{0:N0}}" Width="130" FontWeight="Bold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#D32F2F"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Đóng" Width="100" Height="40" Margin="0,15,0,0"
                HorizontalAlignment="Right" Click="BtnDong_Click" 
                Background="#4C70BA" Foreground="White" BorderThickness="0"/>
    </Grid>
</Window>