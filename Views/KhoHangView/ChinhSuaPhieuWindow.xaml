<Window x:Class="PharmaDistributionApp.Views.KhoHangView.ChinhSuaPhieuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Chỉnh sửa phiếu" Height="680" Width="1000"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=txtMaPhieu}"
        Background="#F3F5F9" FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <Style x:Key="RoundedBtn" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20" MouseDown="Grid_MouseDown" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock x:Name="txtTieuDe" Text="CHỈNH SỬA PHIẾU" FontSize="24" FontWeight="Bold" Foreground="#4C70BA"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Trạng thái:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <ComboBox x:Name="cboTrangThai" Width="150" materialDesign:HintAssist.Hint="Trạng thái">
                    <ComboBoxItem Content="Chờ duyệt"/>
                    <ComboBoxItem Content="Đã duyệt"/>
                    <ComboBoxItem Content="Hoàn thành"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mã phiếu:" FontWeight="Bold" Foreground="Gray" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtMaPhieu" IsReadOnly="True" Background="#F0F0F0" Padding="5"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Ngày lập phiếu:" FontWeight="Bold" Foreground="Gray" Margin="0,0,0,5"/>
                    <DatePicker x:Name="dpNgayLap"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock x:Name="lblDoiTuong" Text="Đối tượng:" FontWeight="Bold" Foreground="Gray" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDoiTuong" Padding="5"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="0" ClipToBounds="True">
            <DataGrid x:Name="dgvChiTiet"
              AutoGenerateColumns="False" 
              CanUserAddRows="False"
              Background="White" 
              BorderThickness="0" 
              RowHeight="50"
              HeadersVisibility="Column" 
              GridLinesVisibility="None"
              SelectionUnit="FullRow" 
              SelectionMode="Single"
              FocusVisualStyle="{x:Null}"
              CellEditEnding="dgvChiTiet_CellEditEnding">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#4C70BA"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="BorderBrush" Value="#6B92D8"/>
                    </Style>

                    <Style TargetType="DataGridCell">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border x:Name="CellBorder" Background="Transparent" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CellBorder" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="CellBorder" Property="Background" Value="#EEEEEE"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="CellBorder" Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style x:Key="EditableCellStyle" TargetType="DataGridCell">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border x:Name="CellBorder" Background="Transparent" BorderThickness="0">
                                        <Grid VerticalAlignment="Center">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
                                            <Border VerticalAlignment="Bottom" 
                                    HorizontalAlignment="Stretch" 
                                    Height="1.5" 
                                    Margin="15,0,15,0"
                                    Background="#4C70BA" 
                                    Opacity="0.4" 
                                    x:Name="UnderlineBorder"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CellBorder" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="CellBorder" Property="Background" Value="#EEEEEE"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="CellBorder" Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Setter TargetName="UnderlineBorder" Property="Opacity" Value="1"/>
                                            <Setter TargetName="UnderlineBorder" Property="Height" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style x:Key="NoBorderEditStyle" TargetType="TextBox">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Foreground" Value="Black"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã thuốc" Binding="{Binding Masp}" Width="100" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Tên thuốc" Binding="{Binding Tensp}" Width="*" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="10,0,0,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Số lượng" 
                            Binding="{Binding Soluong, UpdateSourceTrigger=PropertyChanged}" 
                            CellStyle="{StaticResource EditableCellStyle}" 
                            EditingElementStyle="{StaticResource NoBorderEditStyle}"
                            Width="100"/>

                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding Thanhtien, StringFormat={}{0:N0}}" Width="140" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Thao tác" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignIconButton}" Click="BtnXoaDong_Click" HorizontalAlignment="Center">
                                    <materialDesign:PackIcon Kind="DeleteOutline" Foreground="#D32F2F" Width="22" Height="22"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="TỔNG TIỀN PHIẾU:" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtTongTien" Text="0 đ" FontSize="18" FontWeight="Bold" Foreground="#D32F2F" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy bỏ" Width="100" Height="40" Margin="0,0,10,0" Style="{StaticResource RoundedBtn}" Background="#E0E0E0" Foreground="Black" IsCancel="True" Click="BtnHuy_Click"/>
                <Button Content="Lưu thay đổi" Width="160" Height="40" Style="{StaticResource RoundedBtn}" Background="#4C70BA" Foreground="White" FontWeight="Bold" IsDefault="True" Click="BtnLuu_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>