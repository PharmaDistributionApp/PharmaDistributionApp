<Window x:Class="PharmaDistributionApp.Views.KhoHangView.ChinhSuaTonKho"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Chỉnh sửa tồn kho" Height="420" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F3F5F9"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Window.Resources>
        <Style x:Key="RoundedBtn" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="CHỈNH SỬA TỒN KHO" FontSize="22" FontWeight="Bold" Foreground="#4C70BA" HorizontalAlignment="Center"/>
            <TextBlock Text="Cập nhật số lượng thực tế" FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="25">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.1" Color="#888"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center">
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="PackageVariant" Width="20" Height="20" VerticalAlignment="Center" Foreground="Gray"/>
                    <TextBlock Grid.Column="1" x:Name="txtTenSP" Text="Đang tải..." FontWeight="Bold" FontSize="15" Margin="10,0,0,0" TextWrapping="Wrap"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="BarcodeScanner" Width="20" Height="20" VerticalAlignment="Center" Foreground="Gray"/>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock x:Name="txtMaLovanKho" Text="---" Foreground="#555" FontSize="14"/>
                        <TextBlock Text="(Thông tin định danh)" FontSize="11" Foreground="#999" FontStyle="Italic" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <Separator Background="#EEE" Margin="0,5,0,20"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Số lượng tồn mới:" FontWeight="Bold" Foreground="#4C70BA" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtSoLuong"
                     FontSize="18" FontWeight="Bold" Padding="10" Height="50"
                     VerticalContentAlignment="Center"
                     materialDesign:TextFieldAssist.UnderlineBrush="#4C70BA"
                     BorderBrush="#DDD" BorderThickness="1" Background="#FAFAFA"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Hạn sử dụng mới:" FontWeight="Bold" Foreground="#4C70BA" Margin="0,0,0,8"/>
                        <DatePicker x:Name="dpHSD"
                        FontSize="16" Padding="10" Height="50"
                        VerticalContentAlignment="Center"
                        materialDesign:TextFieldAssist.UnderlineBrush="#4C70BA"
                        BorderBrush="#DDD" BorderThickness="1" Background="#FAFAFA"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="* Lưu ý: Cập nhật trực tiếp vào cơ sở dữ liệu." 
               FontSize="11" Foreground="#D32F2F" Margin="0,15,0,0" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
            <Button Content="Hủy bỏ" Width="110" Height="45" Margin="0,0,15,0" 
                    Style="{StaticResource RoundedBtn}" Background="#E0E0E0" FontWeight="Bold"  Foreground="Black" 
                    IsCancel="True" Click="BtnHuy_Click"/>

            <Button Content="Lưu thay đổi" Width="160" Height="45" 
                    Style="{StaticResource RoundedBtn}" Background="#4C70BA" Foreground="White" FontWeight="Bold"
                    IsDefault="True" Click="BtnLuu_Click"/>
        </StackPanel>
    </Grid>
</Window>