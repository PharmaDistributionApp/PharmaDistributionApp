<UserControl x:Class="PharmaDistributionApp.Views.LoginView.ResetPasswordWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White"
             FontFamily="{DynamicResource MaterialDesignFont}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <UserControl.Resources>
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignOutlinedRevealPasswordBox}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="White"/>

            <Setter Property="materialDesign:HintAssist.IsFloating" Value="False"/>

            <Setter Property="materialDesign:TextFieldAssist.TextFieldCornerRadius" Value="4"/>

            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>

            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#4C70BA"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="80,0">

            <TextBlock Text="ĐỔI MẬT KHẨU" 
                       FontSize="28" FontWeight="Bold" Foreground="#4267B2" 
                       Margin="0,0,0,30" HorizontalAlignment="Left"/>

            <TextBlock Text="Nhập mật khẩu mới" Style="{StaticResource LabelTextStyle}"/>
            <PasswordBox x:Name="txtNewPassword"
                         Style="{StaticResource PasswordBoxStyle}"
                         materialDesign:HintAssist.Hint="************"
                         Margin="0,0,0,20"
                         PasswordChanged="OnPasswordChanged">
                <PasswordBox.Resources>
                    <SolidColorBrush x:Key="PrimaryHueMidBrush" Color="#4C70BA"/>
                </PasswordBox.Resources>
            </PasswordBox>

            <TextBlock Text="Xác nhận mật khẩu" Style="{StaticResource LabelTextStyle}"/>
            <PasswordBox x:Name="txtConfirmPassword"
                         Style="{StaticResource PasswordBoxStyle}"
                         materialDesign:HintAssist.Hint="************"
                         Margin="0,0,0,10"
                         PasswordChanged="OnPasswordChanged">
                <PasswordBox.Resources>
                    <SolidColorBrush x:Key="PrimaryHueMidBrush" Color="#4C70BA"/>
                </PasswordBox.Resources>
            </PasswordBox>

            <TextBlock x:Name="txbErrorMessage" 
                       Text="Thông báo lỗi hiển thị ở đây"
                       Foreground="Red" FontSize="13" FontStyle="Italic"
                       HorizontalAlignment="Left" Margin="0,0,0,10"
                       Visibility="Collapsed"/>

            <Button x:Name="btnSave" Content="LƯU THAY ĐỔI" Margin="0,10,0,0"
                    Height="50" FontSize="16" FontWeight="Bold" 
                    Background="#4C70BA" BorderBrush="{x:Null}"
                    materialDesign:ButtonAssist.CornerRadius="10"
                    Click="btnSave_Click"/>

        </StackPanel>
    </Grid>
</UserControl>