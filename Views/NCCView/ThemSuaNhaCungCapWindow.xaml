<Window x:Class="PharmaDistributionApp.Views.NCCView.ThemSuaNhaCungCapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cập nhật Nhà cung cấp" Height="550" Width="800"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="White" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#888"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Bottom"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelTitle">
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="SectionHeader">
            <Setter Property="Foreground" Value="#6FA6E7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,20,0,15"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <TextBlock Text="THÔNG TIN CƠ BẢN" Style="{StaticResource SectionHeader}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Mã nhà cung cấp (Bắt buộc)" Style="{StaticResource LabelTitle}"/>
                        <TextBox x:Name="txtMaNCC" FontWeight="Bold"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Tên nhà cung cấp (Bắt buộc)" Style="{StaticResource LabelTitle}"/>
                        <TextBox x:Name="txtTenNCC"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="LIÊN HỆ &amp; ĐỊA CHỈ" Style="{StaticResource SectionHeader}" Margin="0,10,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Số điện thoại" Style="{StaticResource LabelTitle}"/>
                        <TextBox x:Name="txtSdt"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Email" Style="{StaticResource LabelTitle}"/>
                        <TextBox x:Name="txtEmail"/>
                    </StackPanel>
                </Grid>

                <StackPanel>
                    <TextBlock Text="Địa chỉ" Style="{StaticResource LabelTitle}"/>
                    <TextBox x:Name="txtDiaChi"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Thoát" IsCancel="True" Click="BtnThoat_Click"
                    Width="120" Height="40" Background="White" Foreground="#555"
                    BorderBrush="#CCC" BorderThickness="1" Margin="0,0,15,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="LƯU DỮ LIỆU" Click="BtnLuu_Click"
                    Width="140" Height="40" Background="#4F7CC9" Foreground="White"
                    BorderThickness="0" FontWeight="Bold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>