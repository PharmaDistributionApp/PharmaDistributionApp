<Window x:Class="PharmaDistributionApp.Views.NCCView.ChiTietNhaCungCapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Chi tiết nhà cung cấp" Height="650" Width="700"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="Transparent" AllowsTransparency="True" WindowStyle="None"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="ValueStyle">
            <Setter Property="Foreground" Value="#2D3436"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ProductTableHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Background" Value="#4C70BA"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4C70BA"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#4C70BA"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" BorderThickness="1" BorderBrush="#EEEEEE">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="2" Opacity="0.15" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#4C70BA" CornerRadius="14,14,0,0" Height="55" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="StoreMarkerOutline" Foreground="White" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="HỒ SƠ NHÀ CUNG CẤP" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Click="BtnDong_Click" Style="{StaticResource MaterialDesignIconForegroundButton}" Foreground="White" Margin="0,0,10,0">
                        <materialDesign:PackIcon Kind="Close"/>
                    </Button>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="30,20,30,10">
                <TextBlock x:Name="lblTenNCC" Text="Tên Nhà Cung Cấp" Foreground="#4C70BA" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"/>
                <Border Background="#F8F9FA" CornerRadius="10" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="15"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã NCC:" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblMaNCC" Text="..." Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="0" Grid.Column="3" Text="SĐT:" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="4" x:Name="lblSdt" Text="..." Style="{StaticResource ValueStyle}" Foreground="#2D3436"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblEmail" Text="..." Style="{StaticResource ValueStyle}"/>

                        <TextBlock Grid.Row="2" Grid.Column="3" Text="Địa chỉ:" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="4" x:Name="lblDiaChi" Text="..." Style="{StaticResource ValueStyle}"/>
                    </Grid>
                </Border>
            </StackPanel>

            <Grid Grid.Row="2" Margin="30,10,30,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <materialDesign:PackIcon Kind="Pill" Foreground="#4C70BA" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Danh mục sản phẩm cung cấp" FontWeight="Bold" FontSize="15" Foreground="#4C70BA"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="dgSanPham" 
                          AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="False"
                          IsReadOnly="True" Background="#fff8f9fa" 
                          BorderThickness="0" 
                          GridLinesVisibility="None" 
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FAFAFA"
                          ColumnHeaderStyle="{StaticResource ProductTableHeaderStyle}"
                          Focusable="True">     
                    <DataGrid.Columns>

                        <DataGridTextColumn Header="Mã SP" Binding="{Binding MASP}" Width="90" FontWeight="Medium">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#4C70BA"/>
<Setter Property="FontWeight" Value="Medium"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TENSP}" Width="*" FontWeight="Medium"/>

                        <DataGridTextColumn Header="ĐVT" Binding="{Binding DVT}" Width="80" FontWeight="Medium">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding GIABAN, StringFormat={}{0:N0} VND}" Width="130" FontWeight="Medium">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Border Grid.Row="3" Background="#F8F9FA" CornerRadius="0,0,14,14" Padding="20,15">
                <Button Content="ĐÓNG" Click="BtnDong_Click" Width="120" Height="38" 
                        Background="#4C70BA" Foreground="White" FontWeight="Bold"
                        BorderThickness="0" materialDesign:ButtonAssist.CornerRadius="19">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                    </Button.Effect>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>